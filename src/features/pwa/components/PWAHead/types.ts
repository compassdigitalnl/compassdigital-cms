/**
 * PWAHead component props.
 *
 * This is a server component that fetches its own data —
 * no props needed at this time.
 */
export interface PWAHeadProps {
  // Reserved for future use (e.g., override themeColor per page)
}
